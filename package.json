{
  "name": "payroll",
  "version": "0.1.0",
  "description": "PAYROLL - Solana Raffle Platform",
  "private": true,
  "workspaces": [
    "app"
  ],
  "scripts": {
    "lint:fix": "npm run lint --workspace=app -- --fix",
    "lint": "npm run lint --workspace=app",
    "dev": "npm run dev --workspace=app",
    "build": "npm run build --workspace=app"
  },
  "dependencies": {
    "@solana/web3.js": "^1.98.4"
  },
  "devDependencies": {
    "prettier": "^3.0.0",
    "typescript": "^5.0.0"
  }
}